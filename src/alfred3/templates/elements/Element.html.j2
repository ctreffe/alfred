<div class="row element d-flex justify-content-{{ position }} {{ hide }} {{ element_class }} {{ align }}" id="elid-{{ name }}" style="{{ height }}">
    <div class="{{ element_width }}">
        {# This is where the element HTML is injected #}
        {{ html | safe}} 
    </div>
</div>